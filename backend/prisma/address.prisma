model Address {
  id        String   @id @default(cuid())
  street    String
  city      String
  number    String
  zipCode   String
  country   String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  person Person?
}
